#include<stdio.h>
struct node 
{ int data;
  struct node* next;
};
int main()
{
    struct node*head;
    int ch;
    switch (ch)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
}